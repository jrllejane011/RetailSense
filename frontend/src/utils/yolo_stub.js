// Stub for YOLO detection in the browser
// Replace with ONNX.js or TensorFlow.js implementation
export async function runYoloOnFrame(imageData) {
  // imageData: ImageData from canvas
  // Return format: [{x1, y1, x2, y2, score, classId}]
  return [];
} 